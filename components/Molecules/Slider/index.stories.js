import React from 'react';
import {storiesOf} from '@storybook/react';
import Slider from './index';

storiesOf('Molecules Slider', module).add('default', () => (
    <div>
        <Slider />
    </div>
));
